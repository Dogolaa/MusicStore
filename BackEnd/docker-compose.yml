services:
  db:
    image: postgres
    container_name: musicstore-db
    environment:
      POSTGRES_DB: musicstore
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: senha
    ports:
      - "5432:5432"